import {
    GameState,
    SlotPanelType,
    InventoryItem,
    Skill,
    Spell,
    PopupState,
} from "../game_state.slint";
import { DragDropItem } from "drag_drop.slint";
import { Theme } from "../theme.slint";
import { HorizontalBox, VerticalBox } from "std-widgets.slint";
import { Icon } from "./icon.slint";
import { PlatformState } from "../platform_state.slint";

component HotBarSlotView inherits Rectangle {
    in property <SlotPanelType> panel-type;
    in property <int> key-num;
    in property <int> slot;
    in property <image> icon;
    in property <int> quantity;
    in property <duration> cooldown-left;
    in property <duration> cooldown-total;
    in property <string> name;
    in property <string> description;
    width: 44px;
    height: 44px;
    background: Theme.surface-muted;
    border-radius: Theme.radius-small;
    border-width: 1px;
    border-color: Theme.border-muted;
    clip: false;
    if icon.width > 0: Rectangle {
        Icon {
            icon: icon;
            cooldown-left: cooldown-left;
            cooldown-total: cooldown-total;
        }

        // Quantity for items
        if quantity > 1: Text {
            text: quantity;
            font-size: 15px;
            font-weight: 700;
            color: Theme.foreground-strong;
            x: parent.width - self.width - 4px;
            y: parent.height - self.height - 2px;
        }
        states [
            dragging when slot-drag.dragging: {
                opacity: 0.5;
            }
        ]
    }
    if key-num >= 0 && key-num <= 11 && !PlatformState.is-android: Text {
        text: key-num < 9 ? key-num + 1 : (key-num == 9 ? "0" : (key-num == 10 ? "-" : "="));
        font-size: Theme.font-size-xsmall;
        color: #808080;
        stroke: #00000080;
        stroke-width: 2px;
        x: 2px;
        y: 1px;
    }
    slot-drag := DragDropItem {
        draggable: icon.width > 0;
        slot-index: slot;
        panel-type: panel-type;
        drag-image: icon;
        drag-image-offset-x: icon.width / 2;
        drag-image-offset-y: icon.height / 2;
        clicked => {
            if PlatformState.is-android && icon.width > 0 {
                GameState.use-action(panel-type, slot);
            }
        }
        double-clicked => {
            if !PlatformState.is-android && icon.width > 0 {
                GameState.use-action(panel-type, slot);
            }
        }
    }

    states [
        hovered when slot-drag.has-hover && icon.width > 0 && !slot-drag.dragging: {
            border-color: Theme.accent;
        }
    ]
    property <bool> has-tooltip: slot-drag.has-hover && icon.width > 0 && !slot-drag.dragging;
    changed has-tooltip => {
        if (root.has-tooltip) {
            PopupState.show(name, self.absolute-position.x, self.absolute-position.y, root.width, root.height);
        } else {
            PopupState.hide();
        }
    }
    states [
        hover when slot-drag.has-hover && !slot-drag.dragging: {
            border-color: Theme.accent-muted;
            background: Theme.surface-secondary;
        }
        drop-hover when slot-drag.has-drag: {
            border-color: Theme.accent;
        }
    ]
}

component HotBarArrowButton inherits Rectangle {
    in property <string> label;
    in property <bool> enabled: true;
    callback activated();
    width: 18px;
    height: 20px;
    background: touch.pressed && enabled ? #505050cc : touch.has-hover && enabled ? #404040a0 : #25252570;
    border-radius: 4px;
    border-width: 1px;
    border-color: enabled ? #60606080 : #30303040;

    Text {
        text: label;
        color: enabled ? Theme.foreground-strong : #606060;
        font-size: Theme.font-size-caption;
        x: (parent.width - self.width) / 2;
        y: (parent.height - self.height) / 2;
    }

    touch := TouchArea {
        mouse-cursor: enabled ? MouseCursor.pointer : MouseCursor.default;
        clicked => {
            if enabled {
                root.activated();
            }
        }
    }
}

component HotBarSelectorButton inherits Rectangle {
    in property <bool> selected: false;
    in property <length> button-height: 6px;
    callback activated();
    width: 16px;
    height: button-height;
    background: {
        if selected {
            @linear-gradient(90deg, #FFC040 0%, #FF8040 100%)
        } else if touch.has-hover {
            #40404080
        } else {
            #30303060
        }
    };
    border-radius: button-height > 10px ? 3px : 2px;
    border-width: 1px;
    border-color: selected ? #FFD06060 : #20202040;

    touch := TouchArea {
        mouse-cursor: MouseCursor.pointer;
        clicked => {
            root.activated();
        }
    }
}

component InventorySlots inherits GridLayout {
    spacing: 4px;
    for idx in 12: HotBarSlotView {
        col: PlatformState.is-android ? Math.mod(idx, 6) : idx;
        row: PlatformState.is-android ? Math.floor(idx / 6) : 0;
        key-num: idx;
        panel-type: SlotPanelType.item;
        slot: GameState.inventory[idx].slot;
        quantity: GameState.inventory[idx].quantity;
        icon: GameState.inventory[idx].icon;
        name: GameState.inventory[idx].name;
    }
}

component SpellSlots inherits GridLayout {
    spacing: 4px;
    for idx in 12: HotBarSlotView {
        col: PlatformState.is-android ? Math.mod(idx, 6) : idx;
        row: PlatformState.is-android ? Math.floor(idx / 6) : 0;
        key-num: idx;
        panel-type: SlotPanelType.spell;
        slot: GameState.spells[idx].slot;
        icon: GameState.spells[idx].icon;
        name: GameState.spells[idx].name;
    }
}

component SkillSlots inherits GridLayout {
    spacing: 4px;
    for idx in 12: HotBarSlotView {
        col: PlatformState.is-android ? Math.mod(idx, 6) : idx;
        row: PlatformState.is-android ? Math.floor(idx / 6) : 0;
        key-num: idx;
        panel-type: SlotPanelType.skill;
        slot: GameState.skills[idx].slot;
        icon: GameState.skills[idx].icon;
        name: GameState.skills[idx].name;
        cooldown-left: GameState.skills[idx].cooldown.time_left;
        cooldown-total: GameState.skills[idx].cooldown.total;
    }
}

component CustomHotBarSlots inherits GridLayout {
    in property <int> offset: 0;
    spacing: 4px;
    for idx in 12: HotBarSlotView {
        col: PlatformState.is-android ? Math.mod(idx, 6) : idx;
        row: PlatformState.is-android ? Math.floor(idx / 6) : 0;
        key-num: idx;
        panel-type: SlotPanelType.hotbar;
        slot: offset + idx;
        icon: GameState.hotbar[offset + idx].icon;
        name: GameState.hotbar[offset + idx].name;
        quantity: GameState.hotbar[offset + idx].quantity;
        cooldown-left: GameState.hotbar[offset + idx].cooldown.time_left;
        cooldown-total: GameState.hotbar[offset + idx].cooldown.total;
    }
}

component DesktopItemSlots inherits GridLayout {
    in property <int> rows: 1;
    spacing: 4px;
    for idx in rows * 12: HotBarSlotView {
        col: Math.mod(idx, 12);
        row: Math.floor(idx / 12);
        key-num: idx;
        panel-type: SlotPanelType.item;
        slot: GameState.inventory[idx].slot;
        quantity: GameState.inventory[idx].quantity;
        icon: GameState.inventory[idx].icon;
        name: GameState.inventory[idx].name;
    }
}

component DesktopSpellSlots inherits GridLayout {
    in property <int> rows: 1;
    spacing: 4px;
    for idx in rows * 12: HotBarSlotView {
        col: Math.mod(idx, 12);
        row: Math.floor(idx / 12);
        key-num: idx;
        panel-type: SlotPanelType.spell;
        slot: GameState.spells[idx].slot;
        icon: GameState.spells[idx].icon;
        name: GameState.spells[idx].name;
    }
}

component DesktopSkillSlots inherits GridLayout {
    in property <int> rows: 1;
    spacing: 4px;
    for idx in rows * 12: HotBarSlotView {
        col: Math.mod(idx, 12);
        row: Math.floor(idx / 12);
        key-num: idx;
        panel-type: SlotPanelType.skill;
        slot: GameState.skills[idx].slot;
        icon: GameState.skills[idx].icon;
        name: GameState.skills[idx].name;
        cooldown-left: GameState.skills[idx].cooldown.time_left;
        cooldown-total: GameState.skills[idx].cooldown.total;
    }
}

component DesktopCustomHotBarSlots inherits GridLayout {
    in property <int> rows: 1;
    in property <int> offset: 0;
    spacing: 4px;
    for idx in rows * 12: HotBarSlotView {
        col: Math.mod(idx, 12);
        row: Math.floor(idx / 12);
        key-num: idx;
        panel-type: SlotPanelType.hotbar;
        slot: offset + idx;
        icon: GameState.hotbar[offset + idx].icon;
        name: GameState.hotbar[offset + idx].name;
        quantity: GameState.hotbar[offset + idx].quantity;
        cooldown-left: GameState.hotbar[offset + idx].cooldown.time_left;
        cooldown-total: GameState.hotbar[offset + idx].cooldown.total;
    }
}

export component HotBar inherits Rectangle {
    property <bool> desktop-expanded: GameState.hotbar-row-count > 1;
    property <int> desktop-row-count: GameState.hotbar-row-count == 5 ? 5 : (GameState.hotbar-row-count == 3 ? 3 : 1);
    width: PlatformState.is-android ? 316px : 632px;
    height: PlatformState.is-android ? 104px : (desktop-row-count == 1 ? 56px : (desktop-row-count == 3 ? 152px : 248px));

    // Subtle fading background
    Rectangle {
        background: @linear-gradient(0deg, #00000025 0%, #00000012 10%, #00000040 20%, #00000040 80%, #00000025 90%, #00000000 100%);
        border-radius: 6px;
    }

    if PlatformState.is-android: HorizontalBox {
        padding: 6px;
        spacing: 4px;

        // Mode selector buttons - minimal dots
        VerticalLayout {
            spacing: PlatformState.is-android ? 2px : 1px;
            alignment: center;
            for i in 6: HotBarSelectorButton {
                selected: GameState.current-hotbar-panel == i;
                button-height: PlatformState.is-android ? 10px : 6px;
                activated => {
                    GameState.set-hotbar-panel(i);
                }
            }

            if PlatformState.is-android: Text {
                text: "\u{270E}";
                font-size: Theme.font-size-body;
                color: #808080;
                stroke: #00000080;
            }
        }

        if GameState.current-hotbar-panel == 0: InventorySlots { }
        if GameState.current-hotbar-panel == 1: SkillSlots { }
        if GameState.current-hotbar-panel == 2: SpellSlots { }
        if GameState.current-hotbar-panel == 3: CustomHotBarSlots {
            offset: 0;
        }
        if GameState.current-hotbar-panel == 4: CustomHotBarSlots {
            offset: 12;
        }
        if GameState.current-hotbar-panel == 5: CustomHotBarSlots {
            offset: 24;
        }
    }

    if !PlatformState.is-android: HorizontalBox {
        padding: 6px;
        spacing: 4px;

        if !desktop-expanded: VerticalLayout {
            spacing: desktop-expanded ? 6px : 2px;
            alignment: center;

            for i in 6: HotBarSelectorButton {
                selected: GameState.current-hotbar-panel == i;
                button-height: 6px;
                activated => {
                    GameState.set-hotbar-panel(i);
                }
            }
        }

        if desktop-expanded: VerticalLayout {
            spacing: 6px;
            alignment: center;

            for i in 4: HotBarSelectorButton {
                selected: i == 3 ? GameState.current-hotbar-panel >= 3 : GameState.current-hotbar-panel == i;
                button-height: 14px;
                activated => {
                    GameState.set-hotbar-panel(i);
                }
            }
        }

        if GameState.current-hotbar-panel == 0: DesktopItemSlots { rows: desktop-row-count; }
        if GameState.current-hotbar-panel == 1: DesktopSkillSlots { rows: desktop-row-count; }
        if GameState.current-hotbar-panel == 2: DesktopSpellSlots { rows: desktop-row-count; }
        if !desktop-expanded && GameState.current-hotbar-panel == 3: DesktopCustomHotBarSlots { rows: 1; offset: 0; }
        if !desktop-expanded && GameState.current-hotbar-panel == 4: DesktopCustomHotBarSlots { rows: 1; offset: 12; }
        if !desktop-expanded && GameState.current-hotbar-panel == 5: DesktopCustomHotBarSlots { rows: 1; offset: 24; }
        if desktop-expanded && GameState.current-hotbar-panel >= 3: DesktopCustomHotBarSlots { rows: desktop-row-count; offset: 0; }

        VerticalLayout {
            spacing: 4px;
            alignment: center;

            HotBarArrowButton {
                label: "▲";
                enabled: GameState.hotbar-row-count < 5;
                activated => {
                    GameState.expand-hotbar();
                }
            }

            HotBarArrowButton {
                label: "▼";
                enabled: GameState.hotbar-row-count > 1;
                activated => {
                    GameState.collapse-hotbar();
                }
            }
        }
    }
}
